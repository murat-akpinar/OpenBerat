# Real values go in .env, which is not committed.
# Filled in during Phase 1: domain, LDAP bind credentials, OIDC client secret,
# Postgres and Redis credentials, ADMIN_GROUP (default OpenBerat-Admins, ADR-0008).
